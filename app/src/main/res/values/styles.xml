<resources>

    <!-- Existing AppTheme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/mp_color_primary</item>
        <item name="colorPrimaryVariant">@color/mp_color_primary_variant</item>
        <item name="colorPrimaryDark">@color/mp_color_primary_dark</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/mp_color_secondary</item>
        <item name="colorSecondaryVariant">@color/mp_color_secondary_variant</item>
        <item name="colorError">@color/mp_color_error</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>

    <!-- Custom Button Style -->
    <style name="RoundedButtonGradient" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">#FFFFFF</item> <!-- Optional: white text color -->
        <item name="android:padding">16dp</item> <!-- Optional: button padding -->
    </style>

    <style name="BottomSheetSpinnerItemStyle">
        <!-- Customize your style as needed -->
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/black</item>
    </style>

    <style name="AppThemeProgressStyle">
        <!-- Customize the style of the progress bar -->
        <item name="android:progressBarStyle">@android:style/Widget.ProgressBar</item>
!-- Optional: Custom progress drawable -->
    </style>

</resources>
